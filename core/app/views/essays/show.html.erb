<div class="container">
  <div class="card">
    <% if @result['error'] %>
      <div class="alert"><%= @result['error'] %></div>
    <% else %>
      <div class="card-header"><h2>Evaluation Results</h2></div>
      
      <div class="score-box">
        <span>Score</span>
        <span class="score-number"><%= @result['score'] %></span>
        <small>/ 3 Points</small>
      </div>

      <div class="score-box">
        <span>Word Count</span>
        <span class="score-number"><%= @result['word_count'] %></span>
      </div>

      <div class="score-box">
        <span>Spelling Errors</span>
        <span class="score-number"><%= @result['spelling_errors'] %></span>
      </div>

      <div class="score-box">
        <span>Grammar Errors</span>
        <span class="score-number"><%= @result['grammar_errors'] %></span>
      </div>
    <% end %>
    
    <%= link_to "Start Over", root_path, class: "btn btn-secondary" %>
  </div>
</div>
