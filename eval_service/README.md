essay evaluation service that connects to the rails web app

### Setup

```bash
pip install -r requirements.txt
```

### Environment variables

FLASK_API_KEY=\<internal secret key that verifies rails app (should be same in \/core env)>
